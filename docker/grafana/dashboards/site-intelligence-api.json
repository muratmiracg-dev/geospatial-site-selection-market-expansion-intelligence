{
  "annotations": {"list": []},
  "editable": false,
  "panels": [
    {
      "type": "timeseries",
      "title": "Request rate",
      "targets": [
        {"expr": "sum(rate(site_intelligence_http_requests_total[5m])) by (path, status)", "legendFormat": "{{path}} {{status}}"}
      ],
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0}
    },
    {
      "type": "timeseries",
      "title": "P95 latency",
      "targets": [
        {"expr": "histogram_quantile(0.95, sum(rate(site_intelligence_http_request_duration_seconds_bucket[5m])) by (le, path))", "legendFormat": "{{path}}"}
      ],
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0}
    }
  ],
  "schemaVersion": 41,
  "tags": ["geospatial", "decision-support"],
  "templating": {"list": []},
  "time": {"from": "now-6h", "to": "now"},
  "title": "Site Intelligence API",
  "uid": "site-intelligence-api",
  "version": 1
}

